'use client';
import {useState} from 'react';
const answers={services:'We handle event management, PR, creative direction and brand experiences—from first idea to final guest.',timeline:'Most projects need 6–12 weeks. Larger productions benefit from 3–6 months, but tell us your date and we’ll be honest about what is possible.',cities:'KULT is based in Pakistan and can produce across Karachi, Lahore, Islamabad and other cities with the right local partners.',quote:'Perfect. Share your event type, city, approximate date and guest count. We’ll help shape the next step.'};
export default function Chatbot(){const [open,setOpen]=useState(false),[messages,setMessages]=useState([{from:'bot',text:'Hey — I’m KULT Concierge. Planning something people should remember?'}]),[value,setValue]=useState('');function ask(key,label){setMessages(m=>[...m,{from:'user',text:label},{from:'bot',text:answers[key]}])}function send(e){e.preventDefault();if(!value.trim())return;setMessages(m=>[...m,{from:'user',text:value},{from:'bot',text:'Got it. For a detailed response, send this brief through our contact form or message @makeitkult on Instagram.'}]);setValue('')}
 return <div className={`chatbot ${open?'chat-open':''}`}><button className="chat-launch" onClick={()=>setOpen(!open)} aria-label="Open KULT concierge"><span>{open?'×':'✦'}</span>{open?'Close':'Ask KULT'}</button>{open&&<div className="chat-panel"><div className="chat-head"><div><b>KULT CONCIERGE</b><small><i/> Online · typically replies soon</small></div><span>K®</span></div><div className="chat-messages">{messages.map((m,i)=><p key={i} className={m.from}>{m.text}</p>)}<div className="quick"><button onClick={()=>ask('services','What do you do?')}>Services</button><button onClick={()=>ask('timeline','How much lead time?')}>Timelines</button><button onClick={()=>ask('cities','Which cities?')}>Cities</button><button onClick={()=>ask('quote','Start a brief')}>Start a brief</button></div></div><form onSubmit={send}><input aria-label="Message" value={value} onChange={e=>setValue(e.target.value)} placeholder="Type your message..."/><button>↗</button></form></div>}</div>}
