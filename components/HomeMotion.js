'use client';
import {useEffect} from 'react';

export default function HomeMotion(){useEffect(()=>{const sections=[...document.querySelectorAll('.motion-section')],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;document.documentElement.classList.add('motion-ready');const reveal=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('section-in');if(entry.target.classList.contains('numbers'))entry.target.querySelectorAll('[data-count]').forEach(node=>animateCount(node));reveal.unobserve(entry.target)}}),{threshold:reduced?0:.12,rootMargin:'0px 0px -8%'});sections.forEach(section=>reveal.observe(section));
 function animateCount(node){const target=Number(node.dataset.count),suffix=node.dataset.suffix||'',pad=Number(node.dataset.pad||0);if(reduced){node.textContent=String(target).padStart(pad,'0')+suffix;return}const start=performance.now(),duration=1100;const tick=now=>{const p=Math.min(1,(now-start)/duration),value=Math.round(target*(1-Math.pow(1-p,3)));node.textContent=String(value).padStart(pad,'0')+suffix;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)}
 const move=e=>{const card=e.target.closest('[data-tilt]');if(!card||matchMedia('(pointer:coarse)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.setProperty('--tilt-y',`${x*7}deg`);card.style.setProperty('--tilt-x',`${y*-6}deg`);card.style.setProperty('--light-x',`${(x+.5)*100}%`);card.style.setProperty('--light-y',`${(y+.5)*100}%`)};const leave=e=>{const card=e.target.closest?.('[data-tilt]');if(card&&!card.contains(e.relatedTarget)){card.style.setProperty('--tilt-y','0deg');card.style.setProperty('--tilt-x','0deg')}};addEventListener('pointermove',move,{passive:true});addEventListener('pointerout',leave,{passive:true});return()=>{reveal.disconnect();document.documentElement.classList.remove('motion-ready');removeEventListener('pointermove',move);removeEventListener('pointerout',leave)}},[]);return null}
